name: Second Workfkflow
on: push
jobs:
    weather-app-start-job:
      runs-on: ubuntu-latest
      steps:
       - name: "welcome"
         run: echo "Welcome to the weather-app. Starting the worklfow job"

    weather-app-end-job:
      runs-on: ubuntu-latest
      steps:
      - name: "bye"
        run: echo "Heelo user. closing the workflow job"

